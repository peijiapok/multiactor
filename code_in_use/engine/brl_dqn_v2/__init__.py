"""BRL-DQN V2 rebuild modules.

These modules live outside the original source tree and import the frozen
simulator as a dependency. Native-BRL-v1 here means a real 4-action DQN policy
with Q-gap-normalized stochastic choice, not a service-floor repair wrapper.
"""

